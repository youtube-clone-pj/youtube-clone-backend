services:
  app:
    image: eclipse-temurin:21-jre
    container_name: spring-app
    ports:
      - "8080:8080"
    volumes:
      - ./app.jar:/app/app.jar
    working_dir: /app
    command: java -jar app.jar
    env_file:
      - .env
